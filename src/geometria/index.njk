---
layout: base.njk
title: Geometria
permalink: /geometria/
---
<section class="page-head"><a class="back" href="/">← Torna alla Home</a><h1>📐 Geometria</h1><p>Forme, misure, spazio e attività per osservare e ragionare.</p></section>
<div class="cards">{% for post in collections.geometria %}<article class="card"><span class="tag">Geometria</span><h3>{{ post.data.title }}</h3><p class="meta">{{ post.data.classe }} · {{ post.data.data }}</p><p>{{ post.data.descrizione }}</p><a class="btn" href="{{ post.url }}">Apri attività</a></article>{% else %}<div class="empty-state">Non ci sono ancora attività pubblicate in Geometria.</div>{% endfor %}</div>
